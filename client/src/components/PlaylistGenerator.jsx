import React from "react";

function PlaylistGenerator({ likedSongs, accessToken }) {
  // Move the component logic here since it was misplaced in pages folder
  // This is the actual component that should be used
  return (
    <div>Playlist Generator Component - Implementation moved to separate file</div>
  );
}

export default PlaylistGenerator;
